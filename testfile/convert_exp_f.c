#include "main.h"

/**
 * replace_e - replaces convert_specs character with string
 * @args: list of arguments
 * @storage: buffer
 * @format: string to be formatted
 * @flag: flag formatter
 * Return: byte number stored
*/

unsigned int replace_e(va_list args, t_buff *storage, const char *format, unsigned char flag)
{

	(void)format;
	(void)args;
	(void)storage;
	(void)flag;

	return (0);
}

/**
 * replace_E - replaces convert_specs character with string
 * @args: list of arguments
 * @storage: buffer
 * @format: string to be formatted
 * @flag: flag formatter
 * Return: byte number stored
*/

unsigned int replace_E(va_list args, t_buff *storage, const char *format, unsigned char flag)
{

        (void)format;
        (void)args;
        (void)storage;
	(void)flag;

        return (0);
}

/**
 * replace_p - replaces convert_specs character with string
 * @args: list of arguments
 * @storage: buffer
 * @format: string to be formatted
 * @flag: flag formatter
 * Return: byte number stored
*/

unsigned int replace_p(va_list args, t_buff *storage, const char *format, unsigned char flag)
{

        (void)format;
        (void)args;
        (void)storage;
	(void)flag;

        return (0);
}

/**
 * replace_f - replaces convert_specs character with string
 * @args: list of arguments
 * @storage: buffer
 * @format: string to be formatted
 * @flag: flag formatter
 * Return: byte number stored
*/

unsigned int replace_f(va_list args, t_buff *storage, const char *format, unsigned char flag)
{

        (void)format;
        (void)args;
        (void)storage;
	(void)flag;

        return (0);
}
